# 仓库里入库的构建产物是二进制。本机 core.autocrlf=true，若不显式声明
# 二进制，git 有机会对它们做行尾转换，checkout 出来就是坏文件。
server/newapi-config-server binary
server/newapi-config-server-linux-amd64 binary
*.exe binary
*.png binary
*.zip binary

# 源码统一按 LF 入库，checkout 时交给各平台自己处理
*.py text eol=lf
*.go text eol=lf
*.ts text eol=lf
*.vue text eol=lf
*.json text eol=lf
*.md text eol=lf
*.sh text eol=lf
# workflow 必须是 LF：run: | 里的 bash heredoc（<<'PY' ... PY）在 CRLF 下
# 结束标记会带上 \r 而匹配不上，脚本直接在 runner 上炸掉
*.yml text eol=lf
*.yaml text eol=lf
