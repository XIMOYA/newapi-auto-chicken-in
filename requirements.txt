# 核心依赖（版本已锁定，勿改为开放区间）
curl_cffi==0.16.0          # TLS/JA3 指纹伪装的 HTTP 客户端，S0/S1 快路径
rich==15.0.0               # 彩色日志与汇总表
PySide6==6.11.1            # Windows 桌面 GUI、托盘和主题
cryptography==49.0.0       # JSON 配置 AES-GCM 加解密

# 浏览器过盾主实现（S2/S3/S4）
# 安装后需执行一次：python -m camoufox fetch
camoufox[geoip]==0.5.4

# 浏览器过盾备选实现（Camoufox 装不上时用）
# 启用后需执行一次：patchright install chromium
patchright==1.61.2

# 测试
pytest==9.1.1
